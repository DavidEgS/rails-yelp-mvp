<h1>Restaurants#index</h1>


<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to "#{restaurant.name}", restaurant_path(restaurant) %>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to 'add an restaurant', new_restaurant_path %>
</p>

